export default class Category {
  constructor(name, statCalculation, skills, subfuntcions) {
    this.name = name;
    this.statCalculation = statCalculation;
    this.skills = skills;
    this.subfunctions = subfunctions;
  }
};
